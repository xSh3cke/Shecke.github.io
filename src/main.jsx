import React from 'react';
import ReactDOM from 'react-dom/client';
import YxStore from './YxStore.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <YxStore />
  </React.StrictMode>
);
